<p id="notice"><%= notice %></p>
<% @posts.each do |post| %>
  <div>
    <% @author = User.where(id: post.user_id)[0]%>
    <%= link_to post.title, post %>
    <%= link_to image_tag(post.picture), post %>
    <%= @author.email %>
  </div>
<% end %>
